---
const navLinks = {
  links: [
    { name: "Home", href: "#" },
    { name: "Process", href: "#process" },
    { name: "Why us?", href: "#why-us" },
    { name: "FAQ", href: "#faq" },
  ],
};
---

<nav
  class="bg-[#333333] fixed w-full z-20 top-0 start-0 border-b border-gray-200"
>
  <div
    class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4"
  >
    <a href="#" class="flex items-center space-x-3">
      <span
        class="self-center text-2xl font-semibold whitespace-nowrap text-white"
      >
        🫔 <i class="text-[#FFD700]">Shaorma</i> . <i class="text-[#D32F2F]"
          >best</i
        >
      </span>
    </a>
    <div class="flex md:order-2 space-x-3">
      <button
        type="button"
        class="text-black bg-[#FFD700] hover:text-[#ffd700] hover:bg-black focus:ring-4 focus:outline-none focus:ring-yellow-300 font-medium rounded-full text-sm px-4 py-2 text-center"
      >
        <a target="_blank" href="https://wa.me/40791252227"> Book a call </a>
      </button>
      <button
        data-collapse-toggle="navbar-sticky"
        type="button"
        class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden hover:bg-[#FFD700] focus:outline-none border border-[#FFD700]focus:ring-2 focus:ring-[#FFD700]"
        aria-controls="navbar-sticky"
        aria-expanded="false"
      >
        <span class="sr-only">Open main menu</span>
        <svg
          class="w-5 h-5"
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 17 14"
        >
          <path
            stroke="currentColor"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M1 1h15M1 7h15M1 13h15"></path>
        </svg>
      </button>
    </div>
    <div
      class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1"
      id="navbar-sticky"
    >
      <ul
        class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-gray-100 rounded-lg hover:text-[#FFD700] md:space-x-8 md:flex-row md:mt-0 md:border-0 text-white bg-[#3333]"
      >
        {
          navLinks.links.map((link) => (
            <li>
              <a
                href={link.href}
                class="block py-2 px-3 text-white rounded hover:bg-[#FFD700] hover:text-black md:hover:text-[#FFD700] md:hover:bg-transparent  md:p-0"
              >
                {link.name}
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</nav>
